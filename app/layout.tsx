import type React from "react"
import "./globals.css"
import type { Metadata } from "next"
import { Inter } from "next/font/google"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "RACE3D | Creation Showcase",
  description: "Browse our collection of high-quality 3D printed figurines and custom designs.",
  keywords: "3D printing, 3d, printer, racerop, racer, figurines, anime, custom designs, collectibles",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}



import './globals.css'